---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "eon_role Resource - terraform-provider-eon"
subcategory: ""
description: |-
  Creates and manages a custom role in Eon. Custom roles define a set of permissions and optional access conditions that restrict which resources the permissions apply to. Built-in roles cannot be created or modified.
---

# eon_role (Resource)

Creates and manages a custom role in Eon. Custom roles define a set of permissions and optional access conditions that restrict which resources the permissions apply to. Built-in roles cannot be created or modified.

## Example Usage

```terraform
terraform {
  required_providers {
    eon = {
      source = "eon-io/eon"
    }
  }
}

# Example: Custom role with permissions
resource "eon_role" "backup_viewer" {
  name = "Backup Viewer (Custom)"

  permission_grants = [
    { permission = "dashboard.view" },
    { permission = "inventory.view" },
  ]
}

# Example: Another custom role
resource "eon_role" "read_only" {
  name = "Read Only (Custom)"

  permission_grants = [
    { permission = "dashboard.view" },
    { permission = "inventory.view" },
    { permission = "jobs.view" },
  ]
}

# Example: IDP group that assigns the custom roles to users in the IdP group
resource "eon_idp_group" "viewers" {
  idp_id            = "your-idp-id"
  provider_group_id = "your-idp-group-id-for-viewers"

  # Reference eon_role resources by ID
  role_ids = [
    eon_role.backup_viewer.id,
    eon_role.read_only.id,
  ]
}

# Optional: Use the eon_roles data source to look up role IDs by name
# data "eon_roles" "all" {}
# Then in eon_idp_group: role_ids = [for r in data.eon_roles.all.roles : r.id if r.name == "Admin"]
```

<!-- schema generated by tfplugindocs -->

## Schema

### Required

- `name` (String) Display name of the role. Must be unique in your Eon account.
- `permission_grants` (Attributes List) List of permissions granted by the role. (see [below for nested schema](#nestedatt--permission_grants))

### Optional

- `access_conditions` (Attributes List) Optional list of access conditions that can be referenced by permission_grants to restrict the scope of permissions. (see [below for nested schema](#nestedatt--access_conditions))

### Read-Only

- `id` (String) System-generated unique identifier for the role.

<a id="nestedatt--permission_grants"></a>

### Nested Schema for `permission_grants`

Required:

- `permission` (String) The permission identifier (e.g. vaults.manage, dashboard.view).

Optional:

- `access_condition_id` (String) Optional ID of an access condition that restricts the resources this permission applies to. Must match the id of an entry in access_conditions.

<a id="nestedatt--access_conditions"></a>

### Nested Schema for `access_conditions`

Required:

- `effect` (String) Effect of the condition (e.g. ALLOW, DENY).
- `id` (String) Unique identifier for this access condition, used in permission_grants.access_condition_id.

Optional:

- `expression` (Attributes) Conditional expression that defines which resources this condition applies to. Same structure as backup policy resource_selector.expression (environment, resource_type, group, data_classes, tag_keys, tag_key_values, etc.). (see [below for nested schema](#nestedatt--access_conditions--expression))

<a id="nestedatt--access_conditions--expression"></a>

### Nested Schema for `access_conditions.expression`

Optional:

- `environment` (Attributes) Environment condition (see [below for nested schema](#nestedatt--access_conditions--expression--environment))
- `group` (Attributes) Group condition with logical operator and operands (see [below for nested schema](#nestedatt--access_conditions--expression--group))
- `resource_type` (Attributes) Resource type condition (see [below for nested schema](#nestedatt--access_conditions--expression--resource_type))

<a id="nestedatt--access_conditions--expression--environment"></a>

### Nested Schema for `access_conditions.expression.environment`

Required:

- `environments` (List of String) List of environments
- `operator` (String) Operator: IN or NOT_IN

<a id="nestedatt--access_conditions--expression--group"></a>

### Nested Schema for `access_conditions.expression.group`

Required:

- `operator` (String) Logical operator: AND or OR

Optional:

- `operands` (Attributes List) List of nested conditions (see [below for nested schema](#nestedatt--access_conditions--expression--group--operands))

<a id="nestedatt--access_conditions--expression--group--operands"></a>

### Nested Schema for `access_conditions.expression.group.operands`

Optional:

- `environment` (Attributes) (see [below for nested schema](#nestedatt--access_conditions--expression--group--operands--environment))
- `resource_type` (Attributes) (see [below for nested schema](#nestedatt--access_conditions--expression--group--operands--resource_type))

<a id="nestedatt--access_conditions--expression--group--operands--environment"></a>

### Nested Schema for `access_conditions.expression.group.operands.environment`

Required:

- `environments` (List of String)
- `operator` (String)

<a id="nestedatt--access_conditions--expression--group--operands--resource_type"></a>

### Nested Schema for `access_conditions.expression.group.operands.resource_type`

Required:

- `operator` (String)
- `resource_types` (List of String)

<a id="nestedatt--access_conditions--expression--resource_type"></a>

### Nested Schema for `access_conditions.expression.resource_type`

Required:

- `operator` (String) Operator: IN or NOT_IN
- `resource_types` (List of String) List of resource types
